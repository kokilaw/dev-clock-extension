{
  "name": "log-time-sync",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:extension": "node scripts/build-extension.js",
    "package:extension": "npm run build:extension && cd dist && zip -r ../log-time-sync-extension.zip .",
    "test:integration": "npm run build:extension && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0"
  }
}
