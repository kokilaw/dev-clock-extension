name: CI

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/ci.yml'
      - 'manifest.json'
      - 'converter-popup.html'
      - 'options.html'
      - 'src/**'
      - 'lib/**'
      - 'icons/**'
      - 'scripts/**'
      - 'tests/**'
      - 'package.json'
      - 'package-lock.json'
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/ci.yml'
      - 'manifest.json'
      - 'converter-popup.html'
      - 'options.html'
      - 'src/**'
      - 'lib/**'
      - 'icons/**'
      - 'scripts/**'
      - 'tests/**'
      - 'package.json'
      - 'package-lock.json'

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  integration-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Resolve package version
        id: pkg
        run: echo "version=$(node -p 'require("./package.json").version')" >> "$GITHUB_OUTPUT"

      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-playwright-

      - name: Install Playwright Chromium
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        run: npx playwright install --with-deps chromium

      - name: Run unit tests
        run: npm run test:unit

      - name: Run integration tests
        run: npm run test:integration:ci

      - name: Build extension package
        run: npm run package:extension

      - name: Prepare snapshot artifact
        run: cp dev-clock-extention-v${{ steps.pkg.outputs.version }}.zip dev-clock-extention-snapshot-${{ github.sha }}.zip

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: integration-test-reports
          retention-days: 7
          path: |
            reports
            playwright-report
          if-no-files-found: ignore

      - name: Upload extension package
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dev-clock-extention-snapshot-${{ github.sha }}
          retention-days: 7
          path: dev-clock-extention-snapshot-${{ github.sha }}.zip
          if-no-files-found: ignore
